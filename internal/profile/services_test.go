package profile

import (
	"testing"
)

func TestUserProfileService(t *testing.T) {
	t.Run("get existent user", func(t *testing.T) {
	})

	t.Run("fails when invalid user", func(t *testing.T) {

	})
}
